# The share card

`instrument-serif.ttf` is the same face the site sets its headlines in, kept
here so the card can be regenerated without a network. Instrument Serif is
published by Rosetta under the SIL Open Font License 1.1, which allows this.

Run `bun scripts/og-image.ts` to rebuild `src/assets/og.png`.
